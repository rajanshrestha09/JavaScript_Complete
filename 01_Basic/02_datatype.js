/* Datatypes:
1. String
2. Number,
3. boolean,
4. null,
5. undefined
6. symbol
7. bigint */


// alert(3+3);

let s_name = "hitest" //string
let age = 18 //number
let isLoggedIn = false //boolean
let state; //undefined

console.table([s_name, age, isLoggedIn, state]);
console.table([typeof s_name, typeof(age), typeof isLoggedIn, typeof state]);
